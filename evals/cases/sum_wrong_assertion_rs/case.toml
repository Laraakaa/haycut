goal = "The test suite fails; find the cause and propose a minimal fix."
verify = "cargo test"
max_steps = 12

[checks]
max_tokens = 20000
expected_markers = ["sum", "4"]
relevant_paths = ["src/lib.rs"]
max_read_radius = 60
